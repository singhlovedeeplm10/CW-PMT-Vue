{{-- 
- DB table structure (- Create a timelines table with fields:
id, title, description, created_at.
- Create a timeline_uploads table with:
id, timeline_id, file_path, type (image/video).)
- Handle multiple file uploads for images and videos.
- Split into two components:
TimelineForm.vue (to add timeline)
TimelineDisplay.vue (to display items).
- Use summernote text editor for the description editor field?
- Can select max 10 images at a single upload
- And set a layout for the maximum images
- And admin can either upload images or videos in a single upload
- Create a like, comment button at the end of every post
- Admin can mention or tag user name in a post or in a comment.


--}}